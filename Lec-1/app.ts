function add (num1: number, num2: number,check:boolean,text:string) {
    if(check)
    {
        console.log(`${text}shivam`);
    
        
    }
    else{
        return num1 + num2;
    }
   };
   const text="hello world"

  console.log(add(10, 20,true,text));
  